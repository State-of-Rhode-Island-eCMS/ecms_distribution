<?php

declare(strict_types = 1);

/**
 * Allow customizable search config on site-by-site basis.
 */

//$char_limit = \Drupal::config('secms_search_settings')->get('char_limit');
//
//$config['name-of-search-module-config']['name-of-character-limit-config-setting'] = $char_limit;
